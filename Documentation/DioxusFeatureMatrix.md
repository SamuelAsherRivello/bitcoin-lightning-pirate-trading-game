# Dioxus Feature Matrix

Keep this file updated as development continues. When routes, platform support, cache behavior, or Dioxus feature usage changes, update the matching rows in the same change.

| # | Feature | Status | Current template usage | Docs |
| - | ------- | ------ | ---------------------- | ---- |
| 1 | Components | ✅ | Shared UI uses `#[component] fn Name(...) -> Element` in components such as [`Page`](../packages/ui/src/client/components/page.rs), [`PageHeader`](../packages/ui/src/client/components/page_header.rs), [`PageFooter`](../packages/ui/src/client/components/page_footer.rs), setup components such as [`WarningCallout`](../packages/ui/src/client/components/setup/mod.rs), game components such as [`GameView`](../packages/ui/src/client/components/game/game_view.rs) and [`HistoryItems`](../packages/ui/src/client/components/game/mod.rs), and routed pages such as [`Home`](../packages/ui/src/client/pages/home.rs), [`SetUp`](../packages/ui/src/client/pages/setup.rs), [`PlayGame`](../packages/ui/src/client/pages/play_game.rs), and [`DebugNetwork`](../packages/ui/src/client/pages/debug_network.rs). `GameView` receives node-derived player/NPC labels, available sats, inventories, layer assets, channel visibility, and callbacks from `PlayGame`; changing location updates only the NPC side while the left player identity, sats, and inventory persist, buy/sell selects the rightmost transferable inventory item for the active owner, and selling is enabled when the current NPC has available sats plus an empty slot. | [Components](https://dioxuslabs.com/learn/0.7/essentials/ui/components/) |
| 2 | RSX markup | ✅ | Pages and components render HTML-like UI through `rsx!`; the active lab pages use direct RSX loops for Home FAQ rows, the Bitcoin vs Lightning comparison table, setup checklists, the layered GameView inventory slots, recent-action history detail pills, invoices, and payments. | [RSX](https://dioxuslabs.com/learn/0.7/essentials/ui/rsx/) |
| 3 | Signals | ✅ | [`AppLayout`](../packages/ui/src/client/mod.rs) provides theme, language, setup profile, lab state, toast, and blocking operation-prompt signals. Lab pages use local signals for forms, busy states, prompt/toast sequence ids, Set Up debug autopilot status, and AutoSend toggles. | [Signals](https://dioxuslabs.com/learn/0.7/essentials/basics/signals/) |
| 4 | Context state | ✅ | The app uses `use_context_provider` and `use_context::<Signal<T>>()` for shared UI state, including subtle happy-path toasts and conspicuous broken/reset prompts. | [Global context](https://dioxuslabs.com/learn/0.7/essentials/basics/context/) |
| 5 | Router | ✅ | [`Route`](../packages/ui/src/client/mod.rs) maps `/` to `Home`, `/setup` to `Set Up`, `/play` to `Play Game`, and `/debug` to `Network Dashboard`; the dashboard includes TRA inventory rows alongside routes, invoices, and payments. | [Routes](https://dioxuslabs.com/learn/0.7/essentials/router/routes/) |
| 6 | Router links | ✅ | [`PageHeader`](../packages/ui/src/client/components/page_header.rs) uses router-aware `Link` components for `Home` and `Set Up`, plus enabled links or disabled button affordances for locked lab routes. | [Navigation](https://dioxuslabs.com/learn/0.7/essentials/router/navigation/) |
| 7 | Asset macro | ✅ | Entry CSS, app-shell CSS, component CSS, flag images, and the Play Game placeholder GameView layer images live under `/assets/...`; the shared [`App`](../packages/ui/src/client/app.rs) links header/footer styles before the routed shell renders to reduce first-paint flicker. | [Assets](https://dioxuslabs.com/learn/0.7/essentials/ui/assets/) |
| 8 | Async actions | ✅ | Lab pages use async event handlers and `spawn` inside `use_effect` to load setup/lab state, verify the Polar bridge before step 1 passes, ensure the named Polar server exists, create/destroy Polar demo nodes, prepare the `Add Tap Root Assets` inventory step, recover stale Polar lab state, open trades, close trades, mine blocks, create invoices, pay invoices, and transfer selected TRA ownership without blocking render. Set Up now includes a debug autopilot runner that directly orchestrates each Polar setup service in visual order, closes its own status prompts, re-verifies the connected lab, honors the Step 2 server-name field when the user clicks `Run`, generates a fresh `autopilot-<timestamp>` server name only when the field is blank or a retry needs a fresh server, continues the current server when resumed mid-flow, and retries Create Nodes with a fresh ensured server if Polar stops listing the selected autopilot network. Play Game route entry explicitly refreshes sat balances and TRA inventory, disables item trading while that refresh is pending, and reports completion through visible status plus toast feedback. Polar setup resets now rewind only local app state while preserving matching verification snapshots so submit can verify and fast-forward already-completed work; Polar bridge/server/node/TRA work stays on submit actions. Happy-path operations report through toasts and broken-lab operations use the blocking prompt surface. | [Async](https://dioxuslabs.com/learn/0.7/essentials/basics/async/) |
| 9 | Effects and timers | ✅ | [`AppLayout`](../packages/ui/src/client/mod.rs) validates and refreshes saved connected Polar setup on every primary route, then polls every second so externally mined Polar blocks update the shared lab state; Play Game also uses route-entry `use_effect` to refetch sat balances and verify TRA inventory when the learner arrives on `/play` or refreshes there; lab pages use `use_effect` for route-entry lab-state hydration and Polar health validation, and [`toast`](../packages/ui/src/client/components/toast.rs) owns platform-specific toast timeout plus 0.25 second minimum prompt-message timing. | [Effects](https://dioxuslabs.com/learn/0.7/essentials/basics/effects/) |
| 10 | Browser local storage | ✅ | [`storage_service`](../packages/ui/src/client/services/storage_service.rs) stores browser theme, language, setup profile, user auth mode/status, the last selected setup Polar nested tab, demo lab state, non-sensitive player identity/session summaries, non-sensitive transaction approval summaries, TRA identity/owner/status snapshots, non-sensitive Game Treasury summaries/history, and template data snapshots in localStorage. It includes snapshot safety checks so browser state does not become a place for Taproot Assets private keys, macaroons, seeds, wallet fingerprints, challenge secrets, approval tokens, or proof material. | [Web platform](https://dioxuslabs.com/learn/0.7/guides/platforms/web/) |
| 11 | Native SQLite cache | ✅ | [`database_service`](../packages/ui/src/client/services/database_service.rs) reads native SQLite data under `data/` for non-wasm builds. | [Async](https://dioxuslabs.com/learn/0.7/essentials/basics/async/) |
| 12 | First-time DB creation | ✅ | [`create_database_if_missing()`](../packages/ui/src/client/services/database_service.rs) is the boilerplate location for first-time native schema creation and seed-row insertion. Normal reads do not recreate or reseed an existing DB. | [Async](https://dioxuslabs.com/learn/0.7/essentials/basics/async/) |
| 13 | Localization | ✅ | [`localization_service`](../packages/ui/src/client/services/localization_service.rs) loads Fluent bundles for English, Spanish, Portuguese, and French. | [Internationalization crate](https://crates.io/crates/dioxus-i18n) |
| 14 | Theme and setup persistence | ✅ | [`storage_service`](../packages/ui/src/client/services/storage_service.rs) persists theme, language, setup profile values, user auth mode/status, setup Polar nested tab preference, and demo lab state for web and desktop. Changing the auth mode clears player identity, current auth session, approval history, and auth warnings before saving the updated profile. | [Signals](https://dioxuslabs.com/learn/0.7/essentials/basics/signals/) |
| 15 | Web and desktop targets | ✅ | [`packages/web`](../packages/web/src/main.rs) and [`packages/desktop`](../packages/desktop/src/main.rs) launch the same shared UI; the root [`index.html`](../index.html) and desktop WebView config use the default dark shell color before the app renders. | [Platforms](https://dioxuslabs.com/learn/0.7/guides/platforms/) |
| 16 | Server contact probe | ✅ | [`DeveloperTools`](../packages/ui/src/client/components/developer_tools.rs) includes a Server button that probes the same-origin web server and reports success or failure through the shared toast. | [Web platform](https://dioxuslabs.com/learn/0.7/guides/platforms/web/) |
| 17 | Forms | ✅ | [`SetUp`](../packages/ui/src/client/pages/setup.rs) uses the `Setup` page heading with `General` and `Connection` second-level sections. `General` owns the `App` / `Mock LNAuth` / `LNAuth` user auth selector, Alby Go testing guidance, and `Sats per transaction`; `Connection` owns the `Polar Connection (Networked)` / `Mock Connection (Offline)` tabs. The Polar tab has nested `1. Environment` and `2. Polar` tabs for install/run guidance, a debug panel before step 1 that shows `Autopilot: on` while the runner owns setup, a user-initiated `Delete all networks` danger action that counts visible Polar networks before showing the `Are you sure?` prompt, and compact form rows for Bridge URL, Server Name, Create Nodes, Game Treasury (Sats), Game Treasury (TRAs), User Nodes (Sats), User Nodes (TRAs), editable Block Height, and Unlock Routes. Each submit step opens the shared operation prompt, updates it during substeps, then closes it before the final toast/focus result. | [Forms](https://dioxuslabs.com/learn/0.7/essentials/fullstack/forms/) |
| 18 | Server-operation boundary | ⚠️ | [`lightning_server_functions`](../packages/ui/src/client/services/lightning_server_functions.rs) mirrors the planned server-function contracts with local async wrappers for the current web/desktop POC state path, including auth session begin/display/mock-approve/complete/cancel calls, Polar bridge calls for server existence/create/start checks, stopped-network validation before Bitcoin RPC/mining, demo-node lifecycle, step 7 app-baseline block-height setting, TRA catalog/verify/reset/mint/transfer calls, catalog-priced invoice creation, and health recovery that locks stale gameplay/debug state. Dioxus `#[post]` macro wiring is deferred because the current `0.7.7` server feature resolves an unavailable `dioxus-liveview = 0.7.7` package in this environment. | [Server functions](https://dioxuslabs.com/learn/0.7/essentials/fullstack/server_functions/) |
| 19 | Lightning service crate | ✅ | [`packages/lightning-service`](../packages/lightning-service/src/lib.rs) exposes client-safe domain DTOs and lab state operations from [`src/client`](../packages/lightning-service/src/client/mod.rs), including auth mode/session/approval models, `TraService` catalog, setup verification, Game Treasury setup/funding/history, NPC starting-item transfer, reset, mint, owner inventory, and transfer rules. Local profile loading, the local auth client boundary, the optional `tonic_lnd` adapter boundary, the server-only Taproot Assets adapter boundary, and native/server lab observer event types live under [`src/server`](../packages/lightning-service/src/server/mod.rs); the current TRA adapter is a local fake boundary ready for Polar/Litd or `tapd` wiring. | [Fullstack](https://dioxuslabs.com/learn/0.7/essentials/fullstack/) |
| 20 | Polar health recovery | ✅ | [`polar_bridge_service`](../packages/ui/src/client/services/polar_bridge_service.rs) classifies bridge, network, Bitcoin backend, and demo-node health from Polar's request/response bridge; its demo service diagnostics support `Off`, `On`, and `Verbose` levels with `On` as the current default, so normal operations log while repeated verification-poll traffic is reserved for `Verbose`. Set Up now presents and autopilots the nine user-facing Polar steps in this order: Bridge URL, Server Name, Create Nodes, Game Treasury (Sats), Game Treasury (TRAs), User Nodes (Sats), User Nodes (TRAs), Block Height, and Unlock Routes. Polar tool docs treat node rename, backend rewiring, and node removal as lifecycle-disruptive operations, so setup recovery inspects first and avoids those mutations when a manually prepared selected network is already usable. Delete-all Polar cleanup deletes networks sequentially and treats success as a final empty network list from the bridge. Unlock Routes revalidates Polar health before marking setup connected, app restart runs a highest-complete-step check that keeps healthy setup connected or rewinds to the needed setup step, every primary route polls connected Polar state, external block-height increases activate pending trades, and broken lab state shows a blocking prompt while keeping the learner on the existing route. | [Async](https://dioxuslabs.com/learn/0.7/essentials/basics/async/) |
| x3 | Dynamic routes | ❌ | The template currently has only static routes. Add dynamic route fields to `Route` when project content needs parameterized pages. | [Dynamic routes](https://dioxuslabs.com/learn/0.7/essentials/router/routes/) |
